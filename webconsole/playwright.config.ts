import { defineConfig, devices } from "@playwright/test";

// The e2e smoke drives the console against a real Franz + Postgres. It boots the
// Vite dev server (which proxies /v1 to the gateway on :8080); starting Franz
// itself is the caller's job (CI: a dedicated job; local: `make e2e`).
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: process.env.CI ? "github" : "list",
  use: {
    baseURL: "http://localhost:5173",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run dev",
    url: "http://localhost:5173",
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
});
