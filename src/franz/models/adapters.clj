(ns franz.models.adapters
  "Adapters transform data from wire to models and models to wire (ADR-001)."
  (:require [franz.models.core :as models]))

;; Define adapter functions here.
;; Example: (defn wire->model [wire-data] ...)
;; Example: (defn model->wire [model] ...)
