(ns franz.logic.core
  "Service/business logic namespaces (ADR-001).
   Pure functions operating on domain models.
   No side effects, no I/O -- all dependencies passed as arguments.")

;; Define business logic here.
