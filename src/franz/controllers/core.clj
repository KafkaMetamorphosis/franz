(ns franz.controllers.core
  "Controllers orchestrate use cases (ADR-001).
   They wire together logic, clients, and adapters
   to fulfill a domain operation.")

;; Define controller functions here.
